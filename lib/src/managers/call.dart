import 'package:injectable/injectable.dart';

/// The `CallManager` class is responsible for managing call operations.
///
/// This class is currently a placeholder and does not contain any specific
/// methods or properties for managing calls. It can be extended in the future
/// to include functionalities related to handling call operations within the
/// portal SDK.

@LazySingleton()
class CallManager {
  /// Creates an instance of `CallManager`.
  ///
  /// This constructor is currently empty, as the class does not have any
  /// initializations or dependencies.
  CallManager();
}
