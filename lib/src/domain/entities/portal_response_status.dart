/// Enumeration representing the status of a portal response.
enum PortalResponseStatus {
  /// The initial state before any response is received.
  initial,

  /// Indicates that the response was successful.
  success,

  /// Indicates that there was an error in the response.
  error,
}
