import 'dart:async';import 'dart:io';import 'package:webitel_portal_sdk/src/domain/entities/media_file_response.dart';/// An abstract interface class representing a file download operation.abstract interface class Download {  /// A stream of [MediaFileResponse] providing updates on the download progress.  StreamController<MediaFileResponse> get onData;  int get offset;  /// Pauses the download operation.  ///  /// This method should be implemented to handle the logic for pausing the download,  /// ensuring that the current state is preserved so the download can be resumed later.  Future<void> pause();  /// Resumes the download operation.  ///  /// This method should be implemented to handle the logic for resuming a paused download,  /// continuing from the last known offset.  Future<void> resume();}