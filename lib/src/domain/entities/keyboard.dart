import 'package:webitel_portal_sdk/src/domain/entities/button.dart';/// Represents a keyboard with a list of buttons.class Keyboard {  /// A list of button rows, where each row is a list of [Button] objects.  /// This allows the keyboard to have a grid of buttons.  final List<List<Button>> buttons;  /// Constructs a [Keyboard] instance with the given list of button rows.  ///  /// [buttons] A required parameter that provides the list of button rows.  Keyboard({    required this.buttons,  });  /// Named constructor for creating an initial [Keyboard] instance with an empty list of buttons.  Keyboard.initial() : buttons = [];}